import Model from './{{camelCase name}}'
import { Router } from 'express'
import { red, green, blue } from './../../helpers/chalk.helper'
const router = new Router()

export function {{camelCase name}}Controller(Collection) {
  // ======
  // Routes
  // ======
  router.get('/', create)
  
  // ======
  // Method
  // ======
  async function index(req, res) {
    try {
      blue('{{camelCase name}}s > controller > index')
     
      const result = 'result'
      green(result)
      res.send(result)
      return
    } catch (error) {
      red(error)
      res.status(500).send(error)
      return
    }
  }

  
  return router
}

export const {{camelCase name}} = router.use('/{{camelCase name}}', {{camelCase name}}Controller(Model))
